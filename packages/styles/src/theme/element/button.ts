import {defaultElementHeight} from './defaultElement';
import {EmblemUIThemeButtonStyles} from './element.interface';


const button: EmblemUIThemeButtonStyles = {
  height: defaultElementHeight,
  horizontalPadding: '12px',
  verticalPadding: '0'
};

export default button;

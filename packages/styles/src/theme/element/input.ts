import {defaultElementHeight} from './defaultElement';
import {EmblemUIThemeInputStyles} from './element.interface';


const input: EmblemUIThemeInputStyles = {
  height: defaultElementHeight,
  horizontalPadding: '10px',
  verticalPadding: '0'
};

export default input;

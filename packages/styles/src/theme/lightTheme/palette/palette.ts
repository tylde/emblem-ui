import {EmblemUIThemePalette} from '../../interface/palette.interface';

import background from './colors/background';
import border from './colors/border';
import danger from './colors/danger';
import dark from './colors/dark';
import disabled from './colors/disabled';
import info from './colors/info';
import primary from './colors/primary';
import secondary from './colors/secondary';
import success from './colors/success';
import warning from './colors/warning';

const palette: EmblemUIThemePalette = {
  background,
  border,
  danger,
  dark,
  disabled,
  info,
  primary,
  secondary,
  success,
  warning
};

export default palette;
